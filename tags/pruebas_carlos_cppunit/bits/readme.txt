C++ classes to manipulate at bit level, that is, access to a chunk of memory that can be written with bit sequences of short but arbitrary length

BitChunk8

BitChunk32

BitBuffer: access to a chunk of memory that can be written with bit sequences of short but arbitrary length.

BitStream: stream wrapper around a BitBuffer

It was designed to work from compression classes that generate pairs (char/int , length) 
